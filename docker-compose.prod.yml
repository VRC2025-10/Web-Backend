services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: "${VRC_BACKEND_IMAGE:-vrc-backend:local}"
    pull_policy: build
    container_name: vrc-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - RUST_LOG=vrc_backend=info,tower_http=info,sqlx=warn
      - BIND_ADDRESS=0.0.0.0:3000
      - BACKEND_BASE_URL=https://vrcapi.arivell-vm.com
      - DATABASE_URL=${DATABASE_URL:?set DATABASE_URL in .env with host db and a URL-encoded password}
      - FRONTEND_ORIGIN=https://vrc10.arivell-vm.com
      - DISCORD_REDIRECT_URI=https://vrcapi.arivell-vm.com/api/v1/auth/discord/callback
      - COOKIE_SECURE=true
      - TRUST_X_FORWARDED_FOR=true
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - gallery_data:/var/lib/vrc/gallery
    networks:
      - internal

  frontend:
    build:
      context: ../Web-Frontend
      dockerfile: Dockerfile
      args:
        INTERNAL_API_URL: http://app:3000
        NEXT_PUBLIC_API_URL: https://vrcapi.arivell-vm.com
        NEXT_PUBLIC_SITE_URL: https://vrc10.arivell-vm.com
        FRONTEND_ORIGIN: https://vrc10.arivell-vm.com
    image: "${VRC_FRONTEND_IMAGE:-vrc-frontend:local}"
    pull_policy: build
    container_name: vrc-frontend
    restart: unless-stopped
    environment:
      - INTERNAL_API_URL=http://app:3000
      - NEXT_PUBLIC_API_URL=https://vrcapi.arivell-vm.com
      - NEXT_PUBLIC_SITE_URL=https://vrc10.arivell-vm.com
      - FRONTEND_ORIGIN=https://vrc10.arivell-vm.com
      - NODE_ENV=production
    depends_on:
      app:
        condition: service_started
    networks:
      - internal

  db:
    image: postgres:16-bookworm
    container_name: vrc-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-vrc_class_reunion}
      POSTGRES_USER: ${POSTGRES_USER:-vrc_app}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-vrc_app} -d ${POSTGRES_DB:-vrc_class_reunion}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - internal
    shm_size: '256mb'
    command:
      - postgres
      - -c
      - shared_buffers=128MB
      - -c
      - work_mem=4MB
      - -c
      - effective_cache_size=512MB
      - -c
      - random_page_cost=1.1
      - -c
      - log_min_duration_statement=100

  caddy:
    image: caddy:2-alpine
    container_name: vrc-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"  # HTTP/3 QUIC
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./secrets/cloudflare-origin.crt:/etc/caddy/certs/cloudflare-origin.crt:ro
      - ./secrets/cloudflare-origin.key:/etc/caddy/certs/cloudflare-origin.key:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      app:
        condition: service_started
      frontend:
        condition: service_healthy
    networks:
      - internal

volumes:
  pgdata:
    driver: local
  gallery_data:
    driver: local
  caddy_data:
    driver: local
  caddy_config:
    driver: local

networks:
  internal:
    driver: bridge
